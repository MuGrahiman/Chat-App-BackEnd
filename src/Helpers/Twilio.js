// import { sendMessage, varifyMessage } from "../Utilities/Twilio.js";

class Twilio {
  constructor() {}
  Message(phoneNo) {
    // return sendMessage(phoneNo);
  }
  Verify(phoneNo) {
    // return varifyMessage(phoneNo);
  }
}

export default Twilio;
